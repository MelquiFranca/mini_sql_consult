const { describe, beforeEach, it } = require('mocha')
const { assert } = require('chai')
const Connection = require('../src/classes/Connection')

describe('#Connection Testes', () => {
    it('', () => {
        Connection
    })
    it('', () => {})
    it('', () => {})
    it('', () => {})
    it('', () => {})
    it('', () => {})
})